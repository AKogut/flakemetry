{
  "name": "@flakemetry/docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm run generate:api && vitepress dev",
    "build": "pnpm run generate:api && vitepress build",
    "preview": "vitepress preview",
    "generate:api": "tsx scripts/generate-api-reference.ts"
  },
  "devDependencies": {
    "@flakemetry/contracts": "workspace:*",
    "tsx": "^4.23.12",
    "vitepress": "^1.6.3",
    "zod": "^4.4.3"
  }
}
